<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="alert alert-danger">
        <h1 class="alert-heading">⚠️ Error</h1>
        <p class="lead">{{message}}</p>
        {{#if error}}
          <hr />
          <p class="mb-0 small text-muted">Detalles: {{error}}</p>
        {{/if}}
        <hr />
        <a href="/" class="btn btn-primary">🏠 Volver al inicio</a>
        <a href="/realtimeproducts" class="btn btn-secondary">⚡ Vista en tiempo
          real</a>
      </div>
    </div>
  </div>
</div>